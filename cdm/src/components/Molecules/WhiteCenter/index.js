import React from 'react';
import {ContainerCenter} from './styles';

export const WhiteCenter = ({children}) =>{
    return(
        <ContainerCenter>
            {children}
        </ContainerCenter>
    )
}